package vine.window;

public interface Window {

    @FunctionalInterface
    interface closeCallback {
        public void onCloseRequest(long context);
    }

    @FunctionalInterface
    interface framebufferSizeCallback {
        public void onFramebufferSizeChange(long context, int width, int height);
    }

    @FunctionalInterface
    interface positionCallback {
        public void onPositionChange(long context, int x, int y);
    }

    @FunctionalInterface
    interface sizeCallback {
        public void onSizeChange(long context, int width, int height);
    }

    void setTitle(String title);

    void init(int height, int width, String name, long sharedContext) throws WindowCreationException;

    boolean isResizeable();

    boolean isVisible();

    boolean isFocused();

    boolean isDecorated();

    boolean isIconified();

    boolean requestedWindowClosing();

    void close();

    void dispose();

    void show();

    long getContextID();

}