package vine.gameplay.entity;

public class NPC extends GameEntity {

    @Override
    public final void update(final float delta) {
        this.position.add(velocity.getX() / 350 * delta, velocity.getY() / 350 * delta, 0);
    }

    /**
     * @param x
     * @param y
     */
    public void construct(final int x, final int y) {
        this.velocity.setX(2);
        this.velocity.setY(10);
    }
}
